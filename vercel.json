{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm prisma generate && pnpm next build",
  "regions": ["sin1"],
  "crons": [{ "path": "/api/worker/tick", "schedule": "0 3 * * *" }]
}
